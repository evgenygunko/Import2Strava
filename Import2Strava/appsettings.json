{
  "Application": {
    "PathToArchive": "C:\\Temp\\endomondo.zip",
    "PathToWorkouts": "C:\\Temp\\Workouts"

    // Note: application token is saved in secret storage. See more info about it here: https://docs.microsoft.com/en-us/aspnet/core/security/app-secrets?view=aspnetcore-5.0&tabs=windows
    // To save the key in the secret storage, run theses commands in the directory with your project file:
    // dotnet user-secrets set "Strava:ClientId" "your_app_clientid"
    // dotnet user-secrets set "Strava:ClientSecret" "your_app_client_secret"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    },
    "Debug": { // Debug provider.
      "LogLevel": {
        "Default": "Debug", // Overrides preceding LogLevel:Default setting.
        "Microsoft.Hosting": "Trace" // Debug:Microsoft.Hosting category.
      }
    }
  }
}